import styled from '@emotion/styled'

export const Container = styled.header`
  display: flex;
  justify-content: center;
  margin-bottom: 40px;

  > img {
    margin: 0 auto;
    width: calc(100% - 80px);
    max-width: 400px;
  }
`
