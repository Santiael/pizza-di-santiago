import styled from '@emotion/styled'

export const Container = styled.header`
  display: flex;
  justify-content: center;
  margin-bottom: 20px;

  > img {
    margin: 0 auto;
    width: calc(100% - 80px);
    max-width: 300px;
  }
`
